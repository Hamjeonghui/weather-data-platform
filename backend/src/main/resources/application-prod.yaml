spring:
  datasource:
    url: jdbc:postgresql://rds-endpoint.amazonaws.com:5432/weather_platform
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate

jwt:
  secret: ${JWT_SECRET}

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGIN}