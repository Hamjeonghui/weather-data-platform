spring:
  datasource:
    url: jdbc:postgresql://rds-endpoint.amazonaws.com:5432/weather_platform
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate

jwt:
  secret: ${JWT_SECRET}

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGIN}

kma:
  service-key: your-service-key
  mid-forecast-url: https://apis.data.go.kr/1360000/MidFcstInfoService/getMidLandFcst