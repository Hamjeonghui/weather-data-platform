# 개발 규칙

- 과도한 DDD나 헥사고날 아키텍처는 사용하지 않는다.
- 기능 중심 패키지 구조를 우선 검토한다.
- Controller는 비즈니스 로직을 직접 처리하지 않는다.
- DTO와 Entity를 직접 노출하지 않는다.
- 모든 외부 입력값은 검증한다.
- 예외 응답 형식을 공통화한다.
- 기능 구현 시 테스트 코드를 함께 작성한다.
- 비밀번호와 API 키는 Git에 커밋하지 않는다.
- 파일을 수정하기 전에 변경 계획과 대상 파일을 먼저 제안한다.